import React from 'react';

import Home from './Home';
import styles from '../styles/root';

const Root = () => (
  <div style={styles}>
    <Home />
  </div>
);

Root.defaultProps = {};

export default Root;
